# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/

# Claude Code local settings (may contain secrets)
.claude/settings.local.json

# SQLite databases
*.db
*.sqlite3

# Embedded MCP server repo (has its own .git and OAuth credentials)
gdrive-mcp/

# OAuth / credential files
credentials.json
token.json
*.pem
*.key

# Kivy / buildozer artifacts
.buildozer/
bin/
