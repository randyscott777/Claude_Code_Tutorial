## Coding Rules

**SQLite3 access:**  
Always use raw SQL statements when using the `sqlite3` module.  
Do not use ORMs (SQLAlchemy, Django ORM, Peewee, etc.) or query builders for SQLite databases in this project.
