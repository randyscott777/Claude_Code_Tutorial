---
name: open-todos
description: Start the todos_list Flask app and open it in the browser. Use when the user says "open todos" or invokes /open-todos.
---

# open-todos skill

## Steps

1. Run the helper script:
   ```
   python .claude/skills/open-todos/open_todos.py
   ```
2. Report the output to the user — either that the app is running and the browser opened, or any error that occurred.
